import 'package:flutter/material.dart';

class AddItemTypePage extends StatelessWidget {
  const AddItemTypePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
